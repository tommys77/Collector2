<common:BindablePage
    x:Class="Collector2.UWP.Views.NewItemPage"
    xmlns:common="using:Collector2.UWP.Common"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converter="using:Collector2.UWP.Converters"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding NewItemViewInstance, Source={StaticResource Locator}}">

    <common:BindablePage.Resources>
        <converter:ByteArrayToImageConverter x:Key="BytesToImage" />
    </common:BindablePage.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center">
            <Image x:Name="SelectedImage" Width="300" Height="400" Source="{Binding SelectedItemImage.ImageBase64, Converter={StaticResource BytesToImage}, Mode=TwoWay }" Margin="0, 5"/>
            <ComboBox x:Name="ActionSelect"
                      Width="300"
                      Margin="0, 5"
                      PlaceholderText="Select an action..."
                      ItemsSource="{Binding Path=ActionSelections}"
                      SelectedItem="{Binding Path=ActionSelected, Mode=TwoWay}"
                      SelectionChangedTrigger="Committed">
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="SelectionChanged">
                        <Core:InvokeCommandAction Command="{Binding ActionSelectedCommand }" />
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
            </ComboBox>
            <TextBlock Text="{Binding Path=SelectedItemImage.Description}" Margin="0, 5" />
            <TextBlock Text="{Binding Path=Status}" Margin="0, 10" />
        </StackPanel>
        
        <Frame
            x:Name="ActionFrame"
            Grid.Column="1"
            SourcePageType="{Binding Path=ActionPage, Mode=TwoWay}"
            Margin="15, 0"
            VerticalAlignment="Stretch">
        </Frame>
    </Grid>
</common:BindablePage>
