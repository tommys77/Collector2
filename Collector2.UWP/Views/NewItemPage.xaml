<Page
    x:Class="Collector2.UWP.Views.NewItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Collector2.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding NewItemViewInstance, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="softwareEditor" Content="Software" Command="{Binding NavigateToSoftwareEditor}" HorizontalAlignment="Center"/>
        <TextBlock x:Name="statusText" Text="{Binding Path=SelectedItemImage.ItemImageId}" VerticalAlignment="Bottom" HorizontalAlignment="Center" />
    </Grid>
</Page>
